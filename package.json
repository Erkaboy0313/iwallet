{
  "name": "iwallet",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind build pipeline for IWALLET (Django backend).",
  "scripts": {
    "build:css": "tailwindcss -i ./static/css/app.css -o ./static/css/build.css --minify",
    "watch:css": "tailwindcss -i ./static/css/app.css -o ./static/css/build.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
